# Browser calls same-origin /api/* which Next rewrites to the backend.
# Set this for `next build` / production if the API is on another host:
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000

# Vapi: set assistant webhook in the dashboard to https://<trycloudflare-host>/api/voice/vapi-webhook (see README).
